<p style="color: green"><%= notice %></p>

<%= render @order_line_item %>

<div>
  <%= link_to "Edit this order line item", edit_order_line_item_path(@order_line_item) %> |
  <%= link_to "Back to order line items", order_line_items_path %>

  <%= button_to "Destroy this order line item", @order_line_item, method: :delete %>
</div>
