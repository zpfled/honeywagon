<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Honeywagon" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Honeywagon">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- Tailwind -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Top navigation bar -->
    <header class="border-b border-gray-200 bg-white">
      <div class="mx-auto max-w-6xl px-4">
        <div class="flex h-16 items-center justify-between">
          <!-- Brand -->
          <div class="flex items-center gap-2">
            <span class="text-lg font-semibold text-gray-900">
              Honeywagon
            </span>
          </div>

          <!-- Nav links -->
          <nav class="flex items-center gap-6 text-sm font-medium">
            <% if user_signed_in? %>
              <%= link_to "Dashboard",
                authenticated_root_path,
                class: "text-gray-700 hover:text-gray-900" %>

              <%= link_to "Orders",
                orders_path,
                class: "text-gray-700 hover:text-gray-900" %>

              <%= link_to "Service Log",
                service_event_reports_path,
                class: "text-gray-700 hover:text-gray-900" %>

              <%= button_to "Sign out",
                destroy_user_session_path,
                method: :delete,
                class: "inline-flex items-center rounded-lg bg-gray-900 px-3 py-2 text-sm font-semibold text-white hover:bg-gray-800" %>
            <% else %>
              <%= link_to "Sign in",
                new_user_session_path,
                class: "text-gray-700 hover:text-gray-900" %>

              <%= link_to "Sign up",
                new_user_registration_path,
                class: "text-gray-700 hover:text-gray-900" %>
            <% end %>
          </nav>
        </div>
      </div>
    </header>

    <% flash_messages = flash.to_hash.slice("notice", "alert", "success", "error", "warning") %>
    <% if flash_messages.values.any?(&:present?) %>
      <div class="mx-auto mt-4 max-w-6xl px-4">
        <% flash_messages.each do |type, message| %>
          <% next if message.blank? %>
          <% styles = case type.to_sym
            when :notice, :success
              "border-green-200 bg-green-50 text-green-900"
            when :alert, :error
              "border-red-200 bg-red-50 text-red-900"
            when :warning
              "border-yellow-200 bg-yellow-50 text-yellow-900"
            else
              "border-gray-200 bg-gray-50 text-gray-900"
          end %>

          <div class="mb-3 rounded-xl border px-4 py-3 text-sm shadow-sm <%= styles %>">
            <%= message %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="container-fluid">
      <%= yield %>
    </main>
  </body>
</html>
