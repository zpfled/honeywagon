<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Dumpr" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Dumpr">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- Tailwind -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Top navigation bar -->
    <header class="bg-brand-primary text-white shadow">
      <div class="mx-auto max-w-6xl px-4">
        <div class="flex h-16 items-center justify-between">
          <!-- Brand -->
          <div class="flex items-center gap-2">
            <% if user_signed_in? %>
              <%= link_to authenticated_root_path, class: "text-lg font-semibold text-white hover:text-white" do %>
                Dumpr
              <% end %>
            <% else %>
              <span class="text-lg font-semibold">
                Dumpr
              </span>
            <% end %>
          </div>

          <!-- Nav links -->
          <nav class="flex items-center gap-6 text-sm font-medium">
            <% if user_signed_in? %>
              <%= link_to "Dashboard",
                authenticated_root_path,
                class: "text-white-soft hover:text-white" %>

              <%= link_to "Routes Calendar",
                calendar_routes_path,
                class: "text-white-soft hover:text-white" %>

              <%= link_to "Orders",
                orders_path,
                class: "text-white-soft hover:text-white" %>

              <div class="relative" data-controller="dropdown">
                <button type="button" data-action="dropdown#toggle" class="inline-flex items-center gap-1 text-white-soft hover:text-white">
                  Operations
                  <svg class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 011.08 1.04l-4.25 4.25a.75.75 0 01-1.08 0L5.21 8.27a.75.75 0 01.02-1.06z" clip-rule="evenodd"/>
                  </svg>
                </button>
                <div data-dropdown-target="menu" class="invisible absolute right-0 mt-2 w-40 transform opacity-0 transition-all duration-150 ease-out">
                  <div class="rounded-xl border border-gray-200 bg-white p-2 shadow-lg">
                    <%= link_to "Orders", orders_path, class: "block rounded-lg px-3 py-2 text-sm text-gray-700 hover:bg-gray-50" %>
                    <%= link_to "Service Log", service_event_reports_path, class: "block rounded-lg px-3 py-2 text-sm text-gray-700 hover:bg-gray-50" %>
                    <%= link_to "Company", edit_company_path, class: "block rounded-lg px-3 py-2 text-sm text-gray-700 hover:bg-gray-50" %>
                  </div>
                </div>
              </div>

              <%= button_to "Sign out",
                destroy_user_session_path,
                method: :delete,
                class: "btn-brand" %>
            <% else %>
              <%= link_to "Sign in",
                new_user_session_path,
                class: "text-white-soft hover:text-white" %>

              <%= link_to "Sign up",
                new_user_registration_path,
                class: "btn-brand--outline" %>
            <% end %>
          </nav>
        </div>
      </div>
    </header>

    <% flash_messages = flash.to_hash.slice("notice", "alert", "success", "error", "warning") %>
    <% if flash_messages.values.any?(&:present?) %>
      <div class="mx-auto mt-4 max-w-6xl px-4">
        <% flash_messages.each do |type, message| %>
          <% next if message.blank? %>
          <% styles = case type.to_sym
            when :notice, :success
              "border-green-200 bg-green-50 text-green-900"
            when :alert, :error
              "border-red-200 bg-red-50 text-red-900"
            when :warning
              "border-yellow-200 bg-yellow-50 text-yellow-900"
            else
              "border-gray-200 bg-gray-50 text-gray-900"
          end %>

          <div class="mb-3 rounded-xl border px-4 py-3 text-sm shadow-sm <%= styles %>">
            <%= message %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="container-fluid">
      <%= yield %>
    </main>

    <%= turbo_frame_tag "customer_modal" %>
    <%= turbo_frame_tag "location_modal" %>
    <%= turbo_frame_tag "rate_plan_modal" %>
    <% report_service_event_id = params[:report_service_event_id].presence %>
    <%= turbo_frame_tag "service_event_modal",
                        src: (report_service_event_id ? new_service_event_report_path(service_event_id: report_service_event_id,
                                                                                       redirect_path: request.path) : nil) %>
    <%= turbo_frame_tag "truck_modal" %>
    <%= turbo_frame_tag "unit_type_modal" %>
    <%= turbo_frame_tag "trailer_modal" %>
    <%= turbo_frame_tag "customer_modal_inline" %>
    <%= turbo_frame_tag "expense_modal" %>
  </body>
</html>
